node_modules/
var/
*.log
.env
.env.*
!.env.example
.DS_Store
dist/
coverage/

# Goldens + stub (plumbing) runs are regenerated by `npm run oracle` / CI.
# Live capability runs (runs/claude-*.json) ARE committed — they are the evidence
# the README results table and gate ratchet read.
goldens/
runs/stub-*.json
var/
